# Ledger: HEAD

## Goal
Add `POST /agent-api/build` endpoint to studio.giselles.ai that delegates to `createBuildHandler` from `@giselles-ai/agent-builder/next-server`, enabling centralized Vercel Sandbox snapshot creation.

## Done
- Added `@giselles-ai/agent-builder@0.1.0` dependency to `apps/studio.giselles.ai/package.json`
- Created `apps/studio.giselles.ai/app/agent-api/build/route.ts` mounting `createBuildHandler` with `AGENT_BUILD_API_TOKEN` env var verification
- Verified: `pnpm install`, `pnpm build-sdk`, type check, and format all pass

## Now
Complete.

## Next
- Set `AGENT_BUILD_API_TOKEN` environment variable in Vercel/production
- Ensure `agent-container` sets `EXTERNAL_AGENT_API_BEARER_TOKEN` to the same value

## Open Questions
- None
